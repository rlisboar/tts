# Pinos = versões instaladas e verificadas no M3 (síntese OK). mlx-* quebram
# com frequência em upgrade — não subir sem testar síntese + transcrição.
mlx-audio==0.4.4
mlx-whisper==0.4.3
mlx-lm==0.31.3
# transformers 5.6+ quebra mlx-lm 0.31.3: AutoTokenizer.register() passa uma
# string como config_class e o transformers novo faz key.__module__ -> derruba o
# import de mlx_lm, o audio_tokenizer (codec) nao carrega e a TTS sai MUDA (zeros).
# 5.5.0 = ultima versao verificada gerando audio real. Nao subir sem testar sintese.
transformers==5.5.0
torch
soundfile
fastapi
uvicorn
python-multipart
# < 2026.08.19: YouTube SABR devolve 403 no download (android_vr default).
yt-dlp>=2026.08.19
imageio-ffmpeg
# backends opcionais (Kokoro / fonemas)
misaki[en]
espeakng-loader
phonemizer-fork
# dev: testes (tests/)
pytest
