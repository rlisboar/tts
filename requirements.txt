# Pinos = versões instaladas e verificadas no M3 (síntese OK). mlx-* quebram
# com frequência em upgrade — não subir sem testar síntese + transcrição.
mlx-audio==0.4.4
mlx-whisper==0.4.3
mlx-lm==0.31.3
# 5.6+ quebrava o mlx-lm antigo (TTS muda, áudio zerado). 5.10.2 verificado
# COM SÍNTESE REAL em 2026-08-30 (RMS 0.08, não mudo) com mlx-lm 0.31.3 —
# o bug não se aplica mais. Re-verificar com áudio real ao subir versão.
transformers==5.10.2
torch
soundfile
fastapi
uvicorn
python-multipart
# < 2026.08.19: YouTube SABR devolve 403 no download (android_vr default).
yt-dlp>=2026.08.19
imageio-ffmpeg
# backends opcionais (Kokoro / fonemas)
misaki[en]
espeakng-loader
phonemizer-fork
# dev: testes + análise estática (nomes indefinidos em funções só explodem em
# runtime — pyflakes pegou 4 desses; rodar após refactors:
#   ./.venv-mlx/bin/python -m pyflakes app.py common.py tts_worker.py backends.py tests/*.py client/mic_router.py
pytest
pyflakes
# TestClient (tests/test_api.py) exige httpx — sem isto, venv novo quebra nos testes
httpx
silero-vad
resemblyzer
parakeet-mlx
